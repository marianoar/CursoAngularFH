
const name: string = 'sarasa';

let hpPoints: number | 'FULL' = 100; // se puede asignar en vez de un tipo un valor!

const isAlive : boolean = true;

hpPoints = 'FULL';

console.log({name, hpPoints, isAlive});


